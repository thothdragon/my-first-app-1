import React from 'react';
import { Layout } from '../components';

const Home = (props) =>
  <Layout>
    <h1>Erreur 404</h1>
    <p>Cette page n'existe pas (encore)...</p>
  </Layout>
;

export default Home;
